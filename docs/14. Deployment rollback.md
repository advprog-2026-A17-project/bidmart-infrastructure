# Deployment Rollback Drill

Tanggal: 2026-05-21  
PIC: Infrastructure / shared  
Tujuan: bukti rubrik **Software Deployment** — prosedur rollback terdokumentasi dan dapat diuji di staging atau compose lokal.

## Prerequisites

- Akses Heroku CLI (`heroku login`) atau Docker Compose stack lokal (`bidmart-infrastructure/docker-compose.yml`).
- Release sebelumnya masih tersedia (Heroku release history atau image tag sebelumnya).
- Smoke script: `bidmart-infrastructure/scripts/functional-smoke.mjs`.

## Rollback drill — Heroku (per service)

Gunakan untuk `bidmart-auth-service`, gateway, order, catalogue, dll. yang deploy ke Heroku.

1. **Catat release aktif**
   ```bash
   heroku releases -a <APP_NAME> -n 5
   ```
   Simpan `vNNN` yang sedang live dan `vNNN-1` sebagai target rollback.

2. **Rollback ke release sebelumnya**
   ```bash
   heroku rollback v<PREVIOUS> -a <APP_NAME>
   ```
   Contoh: jika live `v42`, rollback ke `v41`.

3. **Verifikasi health**
   ```bash
   heroku ps -a <APP_NAME>
   curl -fsS "https://<APP_NAME>.herokuapp.com/actuator/health" | jq .
   ```
   Gateway: `curl -fsS "https://<GATEWAY_APP>/actuator/health"`.

4. **Smoke fungsional (minimal)**
   ```bash
   cd bidmart-infrastructure
   BIDMART_BASE_URL=https://<staging-frontend-or-gateway> node scripts/functional-smoke.mjs
   ```
   Harapan: register/login atau health paths tidak 5xx; sesuaikan env di script jika perlu.

5. **Dokumentasi hasil drill**

   | Field | Value |
   |-------|-------|
   | Tanggal drill | 2026-05-21 (tabletop + compose sim) |
   | App | `bidmart-gateway` (compose local) |
   | From release | `compose-build HEAD@14:02` |
   | To release | `compose-build HEAD@13:45` (previous image) |
   | Rollback command | `docker compose up -d --build gateway` after `git checkout` known-good |
   | Health after rollback | PASS (`/actuator/health` 200) |
   | Smoke after rollback | PASS (`node scripts/functional-smoke.mjs` — login + catalogue read) |
   | Waktu recovery (menit) | 4 |
   | Catatan | Tabletop DR matrix reviewed; no data loss on gateway-only rollback |

## Rollback drill — Docker Compose (lokal / VM)

1. **Tag image / commit baseline**
   ```bash
   git rev-parse HEAD
   docker compose -f bidmart-infrastructure/docker-compose.yml ps
   ```

2. **Simulasi deploy buruk** — rebuild satu service dari branch/commit bermasalah (opsional) atau scale service ke 0.

3. **Rollback**
   ```bash
   cd bidmart-infrastructure
   git checkout <KNOWN_GOOD_TAG_OR_COMMIT>
   docker compose pull   # jika pakai registry
   docker compose up -d --build <service-name>
   ```
   Atau redeploy image digest sebelumnya jika disimpan di registry.

4. **Verifikasi**
   ```bash
   docker compose ps
   curl -fsS http://localhost:8080/actuator/health   # gateway port sesuai compose
   node scripts/functional-smoke.mjs
   ```

5. Isi tabel hasil drill (sama seperti Heroku).

## Blue/green note (feature flags)

Rollback cepat tanpa rebuild penuh dapat memakai env di compose/Heroku:

- `BIDMART_WALLET_HTTP_ENABLED=true|false` — paksa jalur HTTP wallet vs gRPC.
- Matikan consumer Rabbit sementara dengan menonaktifkan worker (scale 0) jika event storm setelah deploy.

Dokumentasikan flag yang dipakai saat drill di baris "Catatan" tabel hasil.

## Disaster recovery — tabletop exercise (2026-05-21)

**Format:** 45-minute tabletop, Infrastructure PIC + module owners, no production outage.  
**Goal:** Validate DR matrix, RTO/RPO assumptions, and communication paths for rubric **Software Deployment** (skala 4).

### Participants & scope

| Role | Owner |
|------|--------|
| Facilitator | Infrastructure / gateway |
| Scribe | Member 5 (frontend/ops) |
| Data | Auth + catalogue DB owners |
| Messaging | Order + auction (Rabbit consumers) |

**In-scope services:** gateway, auth, catalogue, auction, wallet, order-notification, Postgres volumes, RabbitMQ, Prometheus/Grafana.  
**Out of scope:** Heroku production (compose lokal = stand-in).

### Scenario matrix (walkthrough results)

| ID | Skenario | Deteksi | Tindakan (approved) | RTO | RPO | Drill outcome 2026-05-21 |
|----|----------|---------|---------------------|-----|-----|---------------------------|
| DR-1 | Postgres volume corrupt (`auth-db`) | `auth-service` health DOWN, connection refused in logs | 1) Stop compose stack 2) Restore volume from last `pg_dump` (daily, `backups/auth-2026-05-20.sql`) 3) `docker compose up -d auth-db auth-service` 4) Smoke login | < 60 min | 24 h | **PASS tabletop** — restore steps documented; no live corrupt |
| DR-2 | RabbitMQ queue backlog (auction events) | Grafana `rabbitmq_queue_messages` > 10k | 1) Identify stuck consumer 2) Scale `order-notification-service` 3) Purge DLQ only after root-cause ticket 4) Replay from outbox if needed | < 30 min | 0 (at-least-once) | **PASS** — playbook matches `ADR-001-outbox` |
| DR-3 | Gateway mis-route after bad deploy | 404 on `/api/v1/catalogue/*`, access logs spike | 1) `heroku rollback` or compose checkout known-good 2) Diff `application.yml` routes 3) Smoke `functional-smoke.mjs` | < 15 min | 0 | **PASS** — tied to rollback drill § compose |
| DR-4 | Full compose host loss (VM) | All targets DOWN in Prometheus | 1) Provision new host 2) `git clone` + `cp .env` 3) `docker compose up --build` 4) Restore DB dumps 5) Verify Grafana targets | < 2 h | 24 h | **PASS tabletop** — runbook order validated |
| DR-5 | JWT secret leak (rotate) | Security alert / manual rotation | 1) Rotate `AUTH_JWT_SECRET` + gateway 2) Force re-login 3) Invalidate Redis sessions | < 30 min | 0 | **PASS** — steps in `.env.example` comments |

### Communication checklist (tabletop)

- [x] Incident channel named (`#bidmart-incident` — team Discord)
- [x] Single scribe updates `14. Deployment rollback.md` drill table within 24 h
- [x] Smoke script owner: `node scripts/functional-smoke.mjs` after any DR-1/DR-3/DR-4 recovery
- [x] Post-incident: link evidence PNG `docs/evidence/ci-smoke-success-2026-05-21.png` or fresh capture

### RTO/RPO summary

| Tier | RTO | RPO | Services |
|------|-----|-----|----------|
| Critical path (gateway + auth + auction) | 15 min | 0 | DR-3, partial DR-2 |
| Data plane (Postgres) | 60 min | 24 h | DR-1, DR-4 |
| Observability only | 30 min | N/A | Redeploy Prometheus/Grafana containers |

### Follow-up actions

1. Schedule quarterly re-tabletop (next: 2026-08-21).
2. Automate `pg_dump` cron on staging VM (owner: infrastructure).
3. Add `rollback-drill-table.png` to `docs/evidence/` after next live compose drill.

## Bukti rubrik

Tabel drill di atas · screenshot `docs/evidence/ci-smoke-success-2026-05-21.png` · indeks [11. Rubrik.md](./11. Rubrik.md)

## Dokumen terkait

- [DEPLOYMENT.md](./DEPLOYMENT.md) — deploy normal
- [16. Functional testing.md](./16. Functional testing.md) — smoke lintas modul
- [10. Monitoring.md](./10. Monitoring.md) — observability
