# Software Quality

## Testing

Gateway and platform contracts:

| Test | Purpose |
|------|---------|
| `GatewayJwtAuthenticationFilterTest` | JWT parsing, header injection |
| `GatewayRateLimitFilterTest` | Rate limit behavior |
| `GatewayIdentityEnforcementIntegrationTest` | Identity conflict rejection |
| `GatewayAuthIntegrationContractTest` | Auth route proxy |
| `GatewayCatalogueIntegrationContractTest` | Catalogue routes |
| `GatewayOrderIntegrationContractTest` | Order + WS routes |
| `PlatformComposeContractTest` | `docker-compose.yml` service names/ports |
| `CdWorkflowContractTest` | CI workflow expectations |

```bash
./gradlew test
```

## JaCoCo

- `build/reports/jacoco/test/html/index.html`
- Gate: **85%** line minimum in CI (`jacocoTestCoverageVerification`); raise toward 90% as tests expand
- Sonar XML path configured in `sonar { properties { ... } }`

## SonarCloud

| Field | Value |
|-------|--------|
| Project key | `advprog-2026-A17-project_bidmart-infrastructure` |
| Organization | `advprog-2026-a17-project` |
| Config | [`sonar-project.properties`](../sonar-project.properties) + `build.gradle.kts` `sonar { }` |
| CI job | `.github/workflows/ci.yml` → `sonarcloud` (after `test`) |
| Dashboard | https://sonarcloud.io/project/overview?id=advprog-2026-A17-project_bidmart-infrastructure |

**GitHub secret:** `SONAR_TOKEN` on repo `bidmart-infrastructure` (token from SonarCloud → My Account → Security).

If the project is new, the first successful CI scan creates it under the org. Disable **Automatic Analysis** in SonarCloud project settings so CI uploads are authoritative.

## Secure coding (gateway)

- JWT validation before upstream calls.
- Strips/forges prevention on identity headers.
- Internal service token routes separated in policy.
- Rate limiting on sensitive auth endpoints.

## Functional quality (platform)

- [16. Functional testing.md](./16. Functional testing.md) — smoke + Playwright headed flows.
- E2E specs live in `bidmart-frontend/e2e/`.
